<%= form_with(model: mem_hon, local: true) do |form| %>
  <% if mem_hon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mem_hon.errors.count, "error") %> prohibited this mem_hon from being saved:</h2>

      <ul>
        <% mem_hon.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
